# No third-party packages needed at runtime — grading uses the standard library
# (urllib) to call the Claude API. pymupdf/pillow are only used by the
# one-off content-prep scripts, not the app or the serverless function.
